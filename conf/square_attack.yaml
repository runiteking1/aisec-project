defaults:
  - data: mnist
  - _self_

epsilon: 0.1
num_queries: 500  # number of score queries per image
batch_size: 256
output_images: 10
checkpoint_path: 'ERROR'
experiment_name: "square_attack"
seed: 42
